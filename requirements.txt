# Root requirements.txt - combines dependencies from all modules
# Install with: pip install -r requirements.txt

# Scraper dependencies
playwright>=1.40.0
jinja2>=3.1.0

# Translator dependencies
requests>=2.31.0
pyyaml>=6.0
lxml>=4.9.0

# Shared dependencies (used by both scraper and translator)
beautifulsoup4>=4.12.0

# LibreTranslate (for local translation server)
libretranslate>=1.0.0
argostranslate>=1.9.0  # For model management
